{
  "name": "@agent-harness/pi-extensions",
  "version": "0.15.0",
  "private": true,
  "type": "module",
  "description": "TypeScript Pi extensions for the agent harness (bash-guard, protected-files, init, post-edit, stop, failure-log, pre-compact, context-reinject, task-tool).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.0",
    "@earendil-works/pi-ai": "^0.75.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.0",
    "@earendil-works/pi-ai": "^0.75.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
